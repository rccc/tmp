{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
	{{ dump(app.flashes) }}
	{% for message in app.flashes('success') %}
	    <div class="alert alert-success">
	        {{ message }}
	    </div>
	{% endfor %}

	{% for message in app.flashes('error') %}
	    <div class="alert alert-success">
	        {{ message }}
	    </div>
	{% endfor %}

	<div class="row">
	    <div class="col-3"></div>
	    <div class="col-6" data-turbo="false">
{# 		   {{ form_start(form) }}
		        <div class="form-row">
		        	{{ form_widget(form) }}
		        </div>
		        <div class="form-row">
			        <button type="submit" class="btn btn-primary">Upload it!</button>	        	
		        </div>
		    {{ form_end(form) }} #}
	    </div>
	    <div class="col-3"></div>
	</div>
	<div class="row">
		<div class="col-12">
			<div data-controller="datagrid">
				{# <div class="hide-topbar"></div> #}
				<div class="grid-toolbar">
					<input type="file" data-action="change->datagrid#onFileChange" name="files-csv" accept=".csv">
					<label for="csv-file">Browse…</label>
					<button>Save</button>
				</div>
				<div id="grid" ></div>				
			</div>
		</div>
	</div>
{% endblock %}
