<form method="post" action="{{ path('app_data_source_delete', {'id': data_source.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ data_source.id) }}">
    <button class="btn">Delete</button>
</form>
